from django.contrib import admin
# from .models import (function) --mycomments
# admin.site.register(Question) --my comments
# models that are registered appear in the admin site -- my comments
# Register your models here.
